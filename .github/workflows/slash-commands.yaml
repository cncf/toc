name: Slash Commands

on:
  issue_comment:
    types: [created, edited]
  repository_dispatch:
    types:
      - remove-dd/triage-command
      - remove-help-command
      - remove-init-command
      - remove-kind-command
      - remove-level-command
      - remove-review-command
      - remove-sub-command
      - remove-tag-command
      - remove-toc/initiative-command
      - remove-toc-command
      - remove-triage-command
      - remove-vote-command
      - kind-command
      - triage-command
      - tag-command
      - toc-command
      - sub-command
      - dd/triage-command
      - dd/status-command
      - dd/adopters-command
      - dd/gov-review-command
      - dd/tech-review-command
      - dd/sec-review-command
      - toc/initiative-command
      - init-command
      - vote-command
      - help-command

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: slash-command-${{ github.event.issue.number || github.event.client_payload.github.payload.issue.number || github.run_id }}
  cancel-in-progress: true

jobs:
  dispatch-slash-command:
    name: Dispatch Slash Command
    if: ${{ github.event_name == 'issue_comment' }}
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch supported slash commands from issues and PRs
        uses: peter-evans/slash-command-dispatch@v5
        with:
          token: ${{ secrets.SLASH_COMMANDS_PAT }}
          reactions: false
          commands: |
            remove-dd/triage
            remove-help
            remove-init
            remove-kind
            remove-level
            remove-review
            remove-sub
            remove-tag
            remove-toc/initiative
            remove-toc
            remove-triage
            remove-vote
            kind
            triage
            tag
            toc
            sub
            dd/triage
            dd/status
            dd/adopters
            dd/gov-review
            dd/tech-review
            dd/sec-review
            toc/initiative
            init
            vote
            help
          issue-type: both
          permission: write
          allow-edits: true

  process-slash-command:
    name: Process Slash Command
    if: ${{ github.event_name == 'repository_dispatch' }}
    runs-on: ubuntu-latest
    steps:
      - name: Run TOC labeler action for issue/PR command
        uses: cncf/automation/.github/actions/labeler-action@66a552b17f013289688a4e238f7a64b0d8e334a6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          labels: https://raw.githubusercontent.com/cncf/toc/refs/heads/main/.github/labels.yaml
          owner: ${{ github.repository_owner }}
          repo: ${{ github.event.repository.name }}
          issue_number: ${{ github.event.client_payload.github.payload.issue.number }}
          comment_body: ${{ github.event.client_payload.github.payload.comment.body }}
